'use strict';

/**
 * Load Dependencies
 */
const db = require('./db');
const loggerOptions = require('./loggerOptions');

/**
 * Expose to use in other files
 */
module.exports = {
  db,
  loggerOptions,
};
